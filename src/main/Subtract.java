package main;

public class Subtract extends Operation {

    public Subtract(BigInt x, BigInt y) {
        super(x, y);
    }

    @Override
    public BigInt compute(){
        return null;
    }
}
