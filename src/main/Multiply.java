package main;

public class Multiply extends Operation {

    public Multiply(BigInt x, BigInt y) {
        super(x, y);
    }

    @Override
    public BigInt compute(){
        return null;
    }
}
